# Copy this to `.env` and fill in values for local development.
# DO NOT commit your real secret keys to version control.

PORT=5002
NODE_ENV=development
FLUTTERWAVE_PUBLIC_KEY=FLWPUBK_TEST-REPLACE_WITH_YOURS
FLUTTERWAVE_SECRET_KEY=FLWSECK_TEST-REPLACE_WITH_YOURS
FLUTTERWAVE_WEBHOOK_SECRET=FLWSECK_TEST-REPLACE_WITH_YOURS

# SMTP settings for sending ticket emails (fill these with real credentials)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Alternative (Gmail) - use an app password if using Gmail
EMAIL_USER=
EMAIL_PASSWORD=
